<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPersonalApp"
        tools:targetApi="31">
        <activity android:name=".mainApp.MainAppFrontScreen" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".mainApp.WelcomeScreenFirstTime"/>
        <activity android:name=".mainApp.FirstDetailsForm"/>
        <activity android:name=".mainApp.AppDashboard"/>
        <activity android:name=".mainApp.ProfileMainApp"/>
        
    </application>


</manifest>